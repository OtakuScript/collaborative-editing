# 1. 后续工作都根据清单来实行。如果没有相关清单，询问需求并且做计划。且需要把文档列出，补充到项目内。

# 2. 清单的每一项的修改都需要展示出来，如果改动太大的项，需要拆分成更细的功能点来展示。

# 3. 把确认事项的checkbox的确认标识改成√。不用×，×看起来是没通过。比如：
正确的
[ ✅️ ]
错误的
[ ❌️ ]

# 4. 在未获得用户明确确认前，只允许进行只读分析，例如读取文件、搜索代码、查看目录、检查配置。

# 5. 在执行任何会产生副作用的操作前，必须先给出计划并等待用户确认。副作用操作包括但不限于：新增文件、修改文件、删除文件、移动文件、安装依赖、运行格式化、运行测试、启动服务、执行构建、数据库迁移、提交代码、推送代码。

# 6. 只有用户明确说“继续”“执行”“开始”“按计划做”或同等含义的指令，才算确认。模糊回复不算确认。